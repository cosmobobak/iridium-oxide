pub const WIN_SCORE: i32 = 10;
pub const DRAW_SCORE: i32 = 5;

pub const TREE_PRINT_DEPTH: usize = 2;
pub const MAX_NODEPOOL_SIZE: usize = 50_000_000; // convert to use memory limit at some point
pub const ROOT_IDX: usize = 0;

pub const INF: i32 = i32::max_value();
pub const N_INF: i32 = -INF;

pub const EXP_FACTOR: f64 = 10.0;
